# Copy this file to .env and fill in your keys
# Then load with: from dotenv import load_dotenv; load_dotenv()

# No Anthropic key needed — everything runs on local Qwen model
WANDB_API_KEY=your_wandb_api_key_here   # get free at wandb.ai
HF_TOKEN=your_huggingface_token_here    # get free at huggingface.co
